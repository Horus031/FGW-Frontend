export type MajorState = {
  programme: { index: number; id: number };
  year: { index: number; academicYear: string };
  term: { index: number; id: number };
  semester: { index: number; code: string };
  major: { index: number; id: number };
};